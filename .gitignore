# leftovers from the pre-strip pipeline — local only, never commit
leads.db
leads.db-journal
leads.db-wal
leads.db-shm
backup/
.env
markets.json
TODO.md
logs/
weekly_brief.txt

# leads.py's Overpass response cache (24h TTL, --fresh bypasses)
.leads_cache/

venv/
__pycache__/
*.pyc
.pytest_cache/
*.csv

# local session history / OS noise
.remember/
.DS_Store
*.log
