# leads.db is accumulated training data — never commit it
leads.db
leads.db-journal
leads.db-wal
leads.db-shm

.env
weekly_brief.txt
backup/

# private business config/strategy — stays local, examples are tracked
markets.json
TODO.md
logs/
venv/

__pycache__/
*.pyc
.pytest_cache/

# local session history / OS noise — never leaves this machine
.remember/
.DS_Store
